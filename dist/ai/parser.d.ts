/**
 * Parse the AI response to extract just the test code
 * @param aiResponse Raw response from the AI
 * @returns Extracted test code
 */
export declare function parseResponse(aiResponse: string): string;
